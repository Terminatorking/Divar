plugins {
    id("convention.android.feature")
}

android {
    namespace = "ghazimoradi.soheil.divar.category"
    buildFeatures {
        buildConfig = false // Or true to generate BuildConfig
    }
}

dependencies {

}
